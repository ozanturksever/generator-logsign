import * as React from "react";
import { ComponentFactory, link } from "logsign-base-app";
// todo create <%= componentName %> module
import { <%= componentName %> } from "src/moduleNames";

const <%= componentName %>Connected = link({}, () => {
  return <div>content</div>;
});

export default ComponentFactory(<%= componentName %>Connected, {
  moduleName: <%= componentName %>,
  signals: {}
});
